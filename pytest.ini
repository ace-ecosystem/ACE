[pytest]
testpaths = tests
; only run unit tests by default
addopts = --strict -rp -m unit
markers =
    unit
    integration
    system
    functional
    subcutaneous
filterwarnings =
    ignore:::exchangelib[.*]

python_paths = render2/src/shared
